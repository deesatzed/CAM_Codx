# Codex-CAM Methodology — Engineering Handoff Packet (as of 2026-05-26)

Evidence baseline for this packet: branch `feature/initial-impl`, latest commit `c3f520aee77477de18672f5287eddc3503983dd6`, commit date `2026-05-22 12:12:47 -0400`, message `Document MCP-Cortex showpiece RCA`.

Primary repo root: `/Volumes/WS4TB/WS4TBr/CAM_Codx/codex-cam-methodology-impl`.

## 1. Executive Snapshot

Current development phase: local MCP server/product-smoke implementation is green; real interactive/non-interactive Codex E2E remains partially blocked. The repository is on `feature/initial-impl` and tracks `origin/feature/initial-impl`. The last stable commit is `c3f520aee77477de18672f5287eddc3503983dd6` dated `2026-05-22 12:12:47 -0400`.

Working:

- The project builds a standalone four-tool MCP server named `claw-codex-mcp`; the package metadata declares version `0.1.0`, Python `>=3.12`, dependencies `mcp`, `pydantic`, and `sqlite-vec`, and script entry point `cam-codex-mcp` (`pyproject.toml:L5-L29 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- The runtime tool ceiling is exactly four: `cam_recall`, `cam_provenance`, `cam_decisions_search`, and `cam_record_outcome` (`src/claw_codex_mcp/server.py:L24-L48 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- `python -m claw_codex_mcp --version` printed `0.1.0` during this handoff run.
- `pytest tests/codex_mcp/ -q` passed with `68 passed in 2.43s` during this handoff run.
- `python tools/product_smoke.py` passed during this handoff run and verified standalone/connected tools, absent-corpus honesty, connected recall/provenance, decisions search, and idempotent outcome writes.
- `python tools/demo_stdio.py --mode standalone` and `python tools/demo_stdio.py --mode connected` both exited 0 during this handoff run.

Incomplete or blocked:

- Exact model swap to `x-ai/grok-build-0.1` remains blocked under current ChatGPT-backed Codex auth; prior verification returned unsupported-model evidence (`meta/VALIDATION_GAPS_2026-05-20.md:L5-L15 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Full real Codex E2E is still partial: auth/discovery reached MCP, but non-interactive `codex exec` cancelled MCP tool calls with `user cancelled MCP tool call` (`meta/VALIDATION_GAPS_2026-05-20.md:L17-L24 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Fresh-clone standalone install was deferred in the older validation gap record (`meta/VALIDATION_GAPS_2026-05-20.md:L36-L42 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`). TODO: owner?, due?, source? Re-run from GitHub after the current dirty working tree is reconciled.
- Current worktree is not clean: `README.md` is modified and `2026-05-20-104442-command-messagebrainstormingcommand-message.txt` is untracked as of this handoff run.

### 1-Day Resume Checklist

- [ ] `cd /Volumes/WS4TB/WS4TBr/CAM_Codx/codex-cam-methodology-impl`
- [ ] Run `git status --short --branch` and preserve the existing dirty `README.md` plus untracked transcript unless the owner directs otherwise.
- [ ] Run `pytest tests/codex_mcp/ -q`; expected current result is `68 passed`.
- [ ] Run `python tools/product_smoke.py`; expected final line is `PRODUCT SMOKE PASS`.
- [ ] Run `python tools/demo_stdio.py --mode standalone` and `python tools/demo_stdio.py --mode connected`; both should list the same four tools.
- [ ] Review `docs/showpieces/2026-05-22-mcp-cortex-showpiece.md` before claiming native MCP showpiece success.
- [ ] Do not change model/provider config to `x-ai/grok-build-0.1` unless live provider discovery verifies that exact slug.

Active owners:

- TL: TODO: owner?, due?, source?
- PM: TODO: owner?, due?, source?
- DevOps: TODO: owner?, due?, source?
- QA: TODO: owner?, due?, source?
- Security: TODO: owner?, due?, source?
- Current practical owner in repo docs is the solo developer / workspace owner (`PRD.md:L3-L13 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

## 2. Project Specification (What)

Scope and goals:

- Codex-CAM Methodology is a thin librarian bridge between OpenAI Codex CLI and the CAM_CAM mined-methodology corpus, exposed as a standalone MCP server (`README.md:L1-L16 @ feature/initial-impl working tree over c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-26)`).
- The core v1 goals are provenance integrity, append-only fitness/outcome ledger, hard four-tool MCP ceiling, doctrine-conformant skills, keeping the CAM_CAM heavy engine out-of-band, proving Codex actually invokes MCP, honest corpus framing, closing the phantom contract, and adding the CAM librarian doctrine line (`PRD.md:L58-L71 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- The current saved `GOAL.md` for the most recent showpiece required mining MCP-Cortex, recalling newly mined rows through native `cam_cam`, citing provenance, applying one narrow update while preserving four tools, verifying locally, recording outcome, logging evidence, and committing/pushing intentional updates (`GOAL.md:L3-L16 @ feature/initial-impl working tree over c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-26)`).

Non-goals:

- No inline CAM_CAM engine, no daemons, no model dispatch/agent rotation tooling in v1, no `cam_match_failure` in v1, stdio only, no write tools other than `cam_record_outcome`, no silent pattern application, no mock data, no production-ready claim, no `.codex/rules/default.rules` edits, and no modification of the legacy 17-tool MCP (`PRD.md:L74-L89 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Recent showpiece constraints also forbid adding external API requirements to Codex-CAM tests, fabricating corpus rows, direct SQLite proof substitution after native recall is available, weakening tests, or adding a fifth MCP tool (`GOAL.md:L65-L73 @ feature/initial-impl working tree over c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-26)`).

Stakeholders and requirements:

- Primary user is a solo developer using Codex CLI across local repos; PRD names feature-add, failure-rescue, greenfield repo recon, and trivial silent turns as the central scenarios (`PRD.md:L38-L55 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- MCP functional requirements include exact four tools, recall/provenance/search/outcome contracts, stdio config, no `claw_` tool names, standalone connected/degraded modes, and `corpus_status` on every response (`PRD.md:L96-L113 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

Success metrics and baselines:

- PRD success criteria include actual Codex MCP invocation, provenance resolution, lightness, cross-project learning, cold-start improvement, rescue rate, and standalone boot (`PRD.md:L169-L183 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Lightness is recorded as passed locally with legacy RSS `62554112`, new MCP RSS `14614528`, ratio `0.233630` (`meta/VALIDATION_GAPS_2026-05-20.md:L26-L35 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Rescue-rate claims remain waived/deferred until real failure-corpus evidence exists (`docs/REAL_USE_CASE_TEST_PLAN.md:L160-L174 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

## 3. Implementation State (How)

Module/component responsibilities:

| Module | Path | Responsibility | Status |
|---|---|---|---|
| CLI/stdin JSON-RPC entry | `src/claw_codex_mcp/__main__.py` | Handles `--version`, stdio transport, initialize, ping, `tools/list`, and `tools/call`; lazy-loads handlers | green locally |
| Tool registry | `src/claw_codex_mcp/server.py` | Registers the exact four-tool surface and asserts length 4 | green locally |
| Mode and DB helpers | `src/claw_codex_mcp/db.py` | Detects standalone/connected/degraded mode; opens read-only corpus connections; bootstraps outcome schema | green locally |
| Schemas | `src/claw_codex_mcp/schemas.py` | Pydantic v2 input/output models for all tools and corpus statuses | green locally |
| Recall | `src/claw_codex_mcp/tools/recall.py` | FTS5-backed methodology recall with honest absent-corpus behavior | green locally |
| Outcome logging | `src/claw_codex_mcp/tools/record_outcome.py` | Only write path; append-only/idempotent by `run_hash`; stores Cortex-style invocation metadata in evidence | green locally |
| Product smoke | `tools/product_smoke.py` | One-command SDK stdio product smoke across standalone and connected modes | green locally |

Evidence:

- CLI behavior and lazy tool loading are in `src/claw_codex_mcp/__main__.py:L14-L19`, `L24-L42`, `L113-L160`, and `L178-L200 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`.
- Mode detection and outcome schema bootstrap are in `src/claw_codex_mcp/db.py:L92-L130` and `L185-L218 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`.
- Schemas define shared provenance, recall, provenance, decisions, and outcome models with `extra="forbid"` (`src/claw_codex_mcp/schemas.py:L15-L148 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Recall returns absent corpus honestly in standalone mode (`src/claw_codex_mcp/tools/recall.py:L107-L117 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Outcome evidence includes deterministic invocation digest metadata without changing the public output schema (`src/claw_codex_mcp/tools/record_outcome.py:L18-L68 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

Current strategy and boundaries:

- The architecture is split into Codex orchestrator, Codex-CAM thin librarian MCP, and optional CAM_CAM heavy engine; the librarian reads the corpus and writes outcomes but does not decide, mine, evolve, or invoke LLMs (`build_specs.md:L24-L33 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- The boundary rule is: stateful/cross-repo/computational goes to MCP; doctrine/workflow/output schema goes to skills; markdown-sized material remains markdown (`build_specs.md:L48-L55 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

Dependency graph:

```text
Codex CLI
  -> registered cam_cam MCP server over stdio
    -> claw_codex_mcp.__main__
      -> schemas + db mode detection
      -> tools/recall.py            -> optional CAM_CAM claw.db read-only
      -> tools/provenance.py        -> optional CAM_CAM claw.db read-only
      -> tools/decisions_search.py  -> local decisions SQLite FTS index
      -> tools/record_outcome.py    -> append-only codex_outcome_log SQLite
```

Open design decisions:

- Exact real Codex non-interactive MCP approval config remains unresolved (`meta/VALIDATION_GAPS_2026-05-20.md:L17-L24 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Exact availability path for `x-ai/grok-build-0.1` remains unresolved and must not be silently substituted (`meta/VALIDATION_GAPS_2026-05-20.md:L5-L15 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Other PRD open questions include naming, corpus count origin, telemetry/observability, auth token strategy, decisions index scope, ledger schema, ranker diversity, inconclusive outcomes, and SHA resolution across drives (`PRD.md:L203-L217 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

## 4. Architecture & Environments

High-level architecture:

```text
Local developer workspace
  ├── OpenAI Codex CLI + .codex skills/doctrine
  ├── codex-cam-methodology-impl
  │   └── claw_codex_mcp four-tool stdio server
  └── CAM_CAM
      └── data/claw.db optional connected corpus
```

Environment overview:

| Environment | URL | State | Parity gaps |
|---|---|---|---|
| dev/local | local filesystem only | active | Uses local Python env and local SQLite files |
| QA | TODO: owner?, due?, source? | not discovered | No QA environment found |
| staging | TODO: owner?, due?, source? | not discovered | No staging environment found |
| prod | TODO: owner?, due?, source? | not discovered | No production deployment discovered |

URLs, IaC, state backends:

- Git remote is `https://github.com/deesatzed/CAM_Codx.git`.
- No `.github/workflows` directory was found in this repo during this handoff run.
- No Terraform, Pulumi, Helm, Dockerfile, or compose files were discovered in the inspected file list. TODO: owner?, due?, source? Confirm with a deeper infra scan if deployment becomes in scope.
- State is local SQLite: connected mode can use `CAM_CODEX_MCP_DB_PATH`, standalone outcomes default to `~/.cam_codex_mcp/codex_outcome_log.db`, and decisions index defaults to `~/.cam_codex_mcp/codex_decisions_index.db` (`README.md:L137-L147 @ feature/initial-impl working tree over c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-26)`).

Networking:

- Runtime transport is stdio, not HTTP (`build_specs.md:L34-L47 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- No VPC/VNet/ingress/egress/ports/trust-rule documentation was found. TODO: owner?, due?, source? This matters if the MCP server is later exposed beyond local stdio.

## 5. Versions & Compatibility

Languages and SDKs:

| Component | Version | Evidence |
|---|---:|---|
| Python required | `>=3.12` | `pyproject.toml:L5-L17 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)` |
| Python used in this handoff run | `3.13.9` | runtime command output during this handoff |
| `claw-codex-mcp` | `0.1.0` | `src/claw_codex_mcp/__init__.py:L1-L6 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)` |

Frameworks/libraries:

- `mcp>=1.0.0`, `pydantic>=2.0.0`, and `sqlite-vec>=0.1.0` are runtime dependencies (`pyproject.toml:L13-L17 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Dev dependencies include `pytest>=8.0.0`, `pytest-asyncio>=0.24.0`, `pytest-cov>=4.0.0`, `pytest-timeout>=2.3.0`, and `ruff>=0.4.0` (`pyproject.toml:L19-L26 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

Containers:

- None discovered. TODO: owner?, due?, source?

Datastores:

- SQLite corpus: CAM_CAM `claw.db` when connected, with `methodologies`, `methodology_fts`, `methodology_embeddings`, and `methodology_links` referenced by the spec (`build_specs.md:L130-L201` and `L203-L260 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- SQLite outcome ledger: `codex_outcome_log`, `UNIQUE(run_hash)`, and indexes on `ts`, `repo`, and `outcome` (`migrations/001_codex_outcome_log.sql:L1-L21 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

Compatibility matrix:

| Component | Version | Depends On | Compatible With | Notes |
| --------- | ------- | ---------- | --------------- | ----- |
| `claw_codex_mcp` | `0.1.0` | Python `>=3.12`, MCP SDK, Pydantic v2, sqlite-vec | Codex MCP stdio, official MCP SDK client | Local tests pass on Python 3.13.9 |
| Standalone mode | n/a | no `CAM_CODEX_MCP_DB_PATH` | `cam_decisions_search`, `cam_record_outcome`, honest absent recall/provenance | Verified by product smoke and demo |
| Connected mode | n/a | `tests/fixtures/claw_slice.db` or CAM_CAM `claw.db` | all four tools | Verified by product smoke and demo |
| CI | TODO: owner?, due?, source? | TODO | TODO | No workflow file discovered |

## 6. Build & Run Guides

Local dev setup:

```bash
cd /Volumes/WS4TB/WS4TBr/CAM_Codx/codex-cam-methodology-impl
python -m pip install -e ".[dev]"
```

TODO: owner?, due?, source? The install command is inferred from `pyproject.toml` extras; a fresh-clone install was previously deferred in tracked validation gaps (`meta/VALIDATION_GAPS_2026-05-20.md:L36-L42 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

Build commands:

```bash
python -m claw_codex_mcp --version
```

Expected output from this handoff run:

```text
0.1.0
```

Running locally:

```bash
python -m claw_codex_mcp --transport stdio
```

The README states this waits for newline-delimited JSON-RPC on stdin and exits cleanly on EOF (`README.md:L27-L37 @ feature/initial-impl working tree over c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-26)`).

Product smoke:

```bash
python tools/product_smoke.py
```

Expected current result includes:

```text
PASS version 0.1.0
PASS standalone tools cam_decisions_search,cam_provenance,cam_recall,cam_record_outcome
PASS connected tools cam_decisions_search,cam_provenance,cam_recall,cam_record_outcome
PRODUCT SMOKE PASS work_dir=/private/tmp/cam_codex_mcp_product_smoke
```

Demo commands:

```bash
python tools/demo_stdio.py --mode standalone
python tools/demo_stdio.py --mode connected
```

The demo script uses official MCP SDK client over stdio and calls all four tools (`tools/demo_stdio.py:L1-L5`, `L70-L124 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

Common pitfalls:

- Do not pass `top_k`; schemas use `k` and forbid extras (`src/claw_codex_mcp/schemas.py:L37-L45 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Do not treat product smoke as full interactive Codex approval proof; the real-use-case plan explicitly separates SDK smoke from interactive approval behavior (`docs/REAL_USE_CASE_TEST_PLAN.md:L160-L165 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Do not silently substitute a model for `x-ai/grok-build-0.1`; tracked validation says the exact slug is unsupported under current ChatGPT-backed Codex auth (`meta/VALIDATION_GAPS_2026-05-20.md:L5-L15 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

### Local Dev Sanity Checklist

- [ ] `python --version` is compatible with `>=3.12`.
- [ ] `python -m claw_codex_mcp --version` prints `0.1.0`.
- [ ] `pytest tests/codex_mcp/ -q` passes.
- [ ] `python tools/product_smoke.py` passes.
- [ ] `python tools/demo_stdio.py --mode standalone` passes.
- [ ] `python tools/demo_stdio.py --mode connected` passes.
- [ ] `git diff --check` is clean.

## 7. Deployment & Release

Branching and promotion flow:

- Current implementation branch is `feature/initial-impl`, tracking `origin/feature/initial-impl`; last commit is `c3f520a` (`git log` output during this handoff).
- Earlier decision log says implementation runs in a git worktree on `feature/initial-impl`, while the original checkout/main branch stays at the plan commit until merge (`docs/_decision_log.md:L3-L8 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

CI/CD pipelines:

- No `.github/workflows` directory was found in this repo during this handoff run. TODO: owner?, due?, source? CI absence matters because several gates are described as CI-enforced in specs but are currently enforced locally.

Rollback/hotfix procedures:

- TODO: owner?, due?, source? No rollback or hotfix procedure was discovered. This matters if the repo becomes package-published or wired into multiple Codex installs.

Recent releases:

- No package release tags or release notes were discovered. TODO: owner?, due?, source?
- Recent commits include `Document MCP-Cortex showpiece RCA`, `Add MCP-Cortex outcome trace showpiece`, `Log MCP-Cortex Codex-CAM showpiece stop`, `Fix Codex-CAM recall FTS query sanitization`, and `feat(mcp): add product smoke and showpiece test plan` from the local git log during this handoff.

### Pre-Deploy Gate Checklist

- [ ] Worktree clean except intentional release artifacts.
- [ ] `pytest tests/codex_mcp/ -q` passes.
- [ ] `python tools/product_smoke.py` passes.
- [ ] `python tools/demo_stdio.py --mode standalone` passes.
- [ ] `python tools/demo_stdio.py --mode connected` passes.
- [ ] `git diff --check` clean.
- [ ] Skill sanity gate passes: `python tools/validate_skill_frontmatter.py --dir ../.codex/skills`.
- [ ] Deprecated skill references absent: `rg -n "claw_query_memory|claw_store_finding" ../.codex/skills` returns no matches.
- [ ] If changing MCP config/model config, verify provider/model availability live and document exact evidence.

## 8. Configuration, Secrets & Compliance

Config surfaces:

| Surface | Purpose | Evidence |
|---|---|---|
| `CAM_CODEX_MCP_DB_PATH` | Optional corpus DB; presence triggers connected/degraded mode | `README.md:L137-L145 @ feature/initial-impl working tree over c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-26)` |
| `CAM_CODEX_MCP_OUTCOME_DB_PATH` | Optional outcome ledger path | `README.md:L137-L145 @ feature/initial-impl working tree over c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-26)` |
| `CAM_CODEX_MCP_DECISIONS_INDEX` | Optional decisions FTS index path | `README.md:L137-L145 @ feature/initial-impl working tree over c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-26)` |
| `.codex/config.toml` | Workspace MCP wiring | referenced in older handoff, not inspected in this packet |

Secrets handling:

- No secrets were printed or copied into this packet.
- `.codex/rules/default.rules` is explicitly out of scope and must not be modified by this methodology because the PRD identifies secret-bearing content there (`PRD.md:L86-L88 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- TODO: owner?, due?, source? Document vault paths, rotation policy, and least-privilege provisioning steps for any future provider credentials.

SBOM and licenses:

- `LICENSE` exists in the repo file listing from this handoff run, but no SBOM was discovered. TODO: owner?, due?, source?
- License metadata in `pyproject.toml` is `MIT` (`pyproject.toml:L5-L12 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

Data handling requirements:

- No mock/fake/placeholder/simulated/cached corpus rows are allowed; real-use-case policy requires honest absent-corpus behavior and real SQLite corpus data for connected mode (`docs/REAL_USE_CASE_TEST_PLAN.md:L167-L174 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Outcome logging is append-only/idempotent by `run_hash` (`migrations/001_codex_outcome_log.sql:L1-L21 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

### Secrets & Compliance Checklist

- [ ] Do not edit `.codex/rules/default.rules`.
- [ ] Do not commit `.env`, keys, tokens, PHI, or private credentials.
- [ ] Replace secrets in docs with vault/provisioning references.
- [ ] Verify `git status --short --branch` before staging.
- [ ] Confirm no new external API requirement was added to local tests.
- [ ] Confirm connected-mode tests use real/slice SQLite data, not fabricated rows.

## 9. Testing, QA & Quality Gates

Current verification snapshot from this handoff run:

| Command | Result |
|---|---|
| `python --version` | `Python 3.13.9` |
| `python -m claw_codex_mcp --version` | `0.1.0` |
| `pytest tests/codex_mcp/ -q` | `68 passed in 2.43s` |
| `python tools/product_smoke.py` | passed; final line `PRODUCT SMOKE PASS work_dir=/private/tmp/cam_codex_mcp_product_smoke` |
| `python tools/demo_stdio.py --mode standalone` | passed; listed four tools and recorded standalone outcome |
| `python tools/demo_stdio.py --mode connected` | passed; listed four tools, recalled real slice rows, resolved provenance, and recorded connected outcome |
| `git diff --check` | clean at the time it was run before adding this handoff |

Quality gate design:

- Cross-cutting gates include tool-count ceiling, append-only ledger, no phantom MCP references, parseable skill frontmatter, no mock detector, coverage requirements, provenance integrity, and doctrine integrity (`build_to_do_checklist.md:L19-L31 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Test `test_surface_ceiling.py` asserts the exact four-tool set (`tests/codex_mcp/test_surface_ceiling.py:L1-L16 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Product smoke has a pytest wrapper asserting all expected pass markers (`tests/codex_mcp/test_product_smoke.py:L11-L38 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

Known flaky tests:

- TODO: owner?, due?, source? No flaky tests were identified in the inspected evidence. Continue to record any flakes in `meta/VALIDATION_GAPS_*.md` or `docs/_coverage_gaps.md`.

Linters/SAST/DAST:

- Ruff is declared as a dev dependency (`pyproject.toml:L19-L26 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`), but no `ruff` run was performed in this handoff. TODO: owner?, due?, source?
- No SAST/DAST configuration was discovered. TODO: owner?, due?, source?

## 10. Observability & Operations

Dashboards and alert rules:

- TODO: owner?, due?, source? No Grafana/DataDog/CloudWatch/dashboard/alert configuration was discovered. This matters if the MCP server is used outside local stdio.

Runbooks:

- Local runbook is effectively the product smoke and demo commands in this packet plus the real-use-case plan (`docs/REAL_USE_CASE_TEST_PLAN.md:L139-L158 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- No incident runbook was discovered. TODO: owner?, due?, source?

Recent incidents/remediation summaries:

- Model swap blocked because the exact Grok slug is unsupported under current account path (`meta/VALIDATION_GAPS_2026-05-20.md:L5-L15 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Non-interactive Codex MCP approval remains partial (`meta/VALIDATION_GAPS_2026-05-20.md:L17-L24 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- The MCP-Cortex showpiece had false starts around mining location, network validation, and native transport, then completed final native recall/provenance/outcome with outcome ID `b0dfe005-d8d6-4c05-8248-ff86fd854d93` (`docs/showpieces/2026-05-22-mcp-cortex-showpiece.md:L305-L360`, `L443-L543 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

## 11. Work Stream & Next Steps

Top 5 next steps:

| Goal | Owner | ETA guess | Risk | Evidence |
|---|---|---:|---|---|
| Reconcile dirty working tree and decide whether to commit current `README.md` modification and transcript | TODO: owner?, due?, source? | 1 | med | current `git status` from this handoff |
| Run real Codex E2E in interactive TUI with MCP approvals, or find supported non-interactive approval config | TODO: owner?, due?, source? | 1 | high | `meta/VALIDATION_GAPS_2026-05-20.md:L17-L24` |
| Fresh-clone install and standalone smoke from GitHub | TODO: owner?, due?, source? | 1 | med | `meta/VALIDATION_GAPS_2026-05-20.md:L36-L42` |
| Add/verify CI workflow for pytest, product smoke, skill sanity, and diff check | TODO: owner?, due?, source? | 2 | med | no workflow discovered |
| Decide model policy: keep subscription-backed current model or provide verified provider path for `x-ai/grok-build-0.1` | TODO: owner?, due?, source? | 1 | high | `meta/VALIDATION_GAPS_2026-05-20.md:L5-L15` |

Blocked items and dependencies:

- Exact Grok model slug requires a different verified provider/account path or a future Codex subscription path (`meta/VALIDATION_GAPS_2026-05-20.md:L5-L15 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Real Codex E2E requires interactive approval behavior or a supported config key for non-interactive MCP approvals (`meta/VALIDATION_GAPS_2026-05-20.md:L17-L24 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

Risk matrix:

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Phantom MCP references return | med | high | Keep skill grep and validator gates in pre-deploy checklist |
| Model availability claims drift | high | med | Verify live before config changes; keep unsupported slug documented |
| Product smoke mistaken for full Codex E2E | med | high | Keep local SDK vs interactive Codex distinction in docs |
| Corpus data fabrication under pressure | med | high | Preserve no-mock/no-fake policy and connected slice fixture tests |
| Dirty worktree staging leaks unrelated files | med | med | Stage narrowly and inspect `git diff --cached` before commit |

Decision log / ADR index:

- Worktree-based execution and build target mode decisions are in `docs/_decision_log.md:L3-L14 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`.
- Plan amendments, corpus fixture reality, coverage waivers, model swap block, and lightness remediation are recorded across `docs/_decision_log.md:L31-L220 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`.

## 12. Appendices

API surfaces:

| Tool | Purpose | Input notes |
|---|---|---|
| `cam_recall` | Top-K methodologies for a natural-language query | `query`, `k`, filters; extras forbidden |
| `cam_provenance` | Provenance envelope for a methodology ID | `methodology_id`, optional solution/links |
| `cam_decisions_search` | Search indexed `DECISIONS.md` records | `query`, `k`, optional repo/since |
| `cam_record_outcome` | Append-only outcome logging | `methodology_ids`, `task_id`, `repo`, `outcome`, `evidence`, `run_hash`, `notes` |

Schema evidence: `src/claw_codex_mcp/schemas.py:L37-L148 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`.

Data model:

- `codex_outcome_log(id, methodology_ids, task_id, repo, outcome, evidence, ts, run_hash, notes)`, with `UNIQUE(run_hash)` and indexes on timestamp, repo, outcome (`migrations/001_codex_outcome_log.sql:L5-L21 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Methodology/provenance model comes from `methodologies`, `methodology_fts`, `methodology_embeddings`, and `methodology_links` per build spec (`build_specs.md:L130-L260 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).

Migrations:

- Last known migration file: `migrations/001_codex_outcome_log.sql`.
- Pending migrations: TODO: owner?, due?, source?
- Rollback notes: none discovered; append-only semantics mean rollback should be treated as environment restore rather than row mutation. TODO: owner?, due?, source?

Glossary:

- Thin librarian: MCP reads methodology corpus and records outcomes; it does not mine or decide (`build_specs.md:L24-L33 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Corpus status: `connected`, `empty`, `absent`, or `degraded`, present on tool responses (`build_specs.md:L34-L47 @ feature/initial-impl#c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-22)`).
- Flywheel: recall, cite, apply, verify, record outcome, improve future recall; named in README glossary (`README.md:L212-L235 @ feature/initial-impl working tree over c3f520aee77477de18672f5287eddc3503983dd6 (2026-05-26)`).

```json
{
  "project_name": "Codex-CAM Methodology",
  "pause_date_iso": "2026-05-26",
  "repo_roots": ["/Volumes/WS4TB/WS4TBr/CAM_Codx/codex-cam-methodology-impl", "https://github.com/deesatzed/CAM_Codx.git"],
  "default_branch": "feature/initial-impl",
  "last_stable_commit": {"sha": "c3f520aee77477de18672f5287eddc3503983dd6", "date": "2026-05-22T12:12:47-04:00", "message": "Document MCP-Cortex showpiece RCA"},
  "modules": [
    {"name": "CLI stdio entry", "path": "src/claw_codex_mcp/__main__.py", "owner": "TODO: owner?", "status": "green", "notes": "Version and stdio demos passed locally."},
    {"name": "MCP tool registry", "path": "src/claw_codex_mcp/server.py", "owner": "TODO: owner?", "status": "green", "notes": "Exactly four registered tools."},
    {"name": "DB and mode helpers", "path": "src/claw_codex_mcp/db.py", "owner": "TODO: owner?", "status": "green", "notes": "Standalone and connected smoke passed."},
    {"name": "Tool schemas", "path": "src/claw_codex_mcp/schemas.py", "owner": "TODO: owner?", "status": "green", "notes": "Pydantic v2 schemas forbid extra fields."},
    {"name": "Product smoke", "path": "tools/product_smoke.py", "owner": "TODO: owner?", "status": "green", "notes": "Passed during handoff generation."},
    {"name": "Interactive Codex E2E", "path": "../.codex/config.toml", "owner": "TODO: owner?", "status": "yellow", "notes": "Discovery/auth reached MCP, but non-interactive approval remains blocked."}
  ],
  "versions": {
    "languages": [{"name": "Python", "version": "3.13.9"}],
    "frameworks": [{"name": "mcp", "version": ">=1.0.0"}, {"name": "pydantic", "version": ">=2.0.0"}, {"name": "sqlite-vec", "version": ">=0.1.0"}],
    "containers": [],
    "datastores": [{"engine": "sqlite", "version": "TODO: owner?", "migration_head": "001_codex_outcome_log"}]
  },
  "ci_cd": {
    "pipelines": [{"name": "TODO: CI workflow not discovered", "status": "failing", "url": "TODO: owner?"}],
    "release_flow": "other"
  },
  "environments": [
    {"name": "dev", "url": "local filesystem", "parity_gaps": ["No QA/staging/prod environment discovered", "Interactive Codex E2E approval unresolved"]},
    {"name": "qa", "url": "TODO: owner?", "parity_gaps": ["Environment not discovered"]},
    {"name": "staging", "url": "TODO: owner?", "parity_gaps": ["Environment not discovered"]},
    {"name": "prod", "url": "TODO: owner?", "parity_gaps": ["Environment not discovered"]}
  ],
  "observability": {
    "dashboards": [{"name": "TODO: dashboards not discovered", "url": "TODO: owner?"}],
    "alerts": [{"name": "TODO: alerts not discovered", "url": "TODO: owner?", "status": "quiet"}]
  },
  "backlog_next_steps": [
    {"title": "Reconcile dirty README and untracked transcript", "owner": "TODO: owner?", "eta_days": 1, "risk": "med"},
    {"title": "Run real interactive Codex E2E with MCP approvals", "owner": "TODO: owner?", "eta_days": 1, "risk": "high"},
    {"title": "Fresh-clone install and standalone smoke", "owner": "TODO: owner?", "eta_days": 1, "risk": "med"},
    {"title": "Add CI workflow for local gates", "owner": "TODO: owner?", "eta_days": 2, "risk": "med"},
    {"title": "Resolve model policy for x-ai/grok-build-0.1", "owner": "TODO: owner?", "eta_days": 1, "risk": "high"}
  ],
  "risks": [
    {"risk": "Non-interactive Codex MCP tool approval remains unresolved.", "likelihood": "high", "impact": "high", "mitigation": "Run interactive TUI proof or identify supported approval config key."},
    {"risk": "Unsupported model slug is accidentally configured as active default.", "likelihood": "med", "impact": "med", "mitigation": "Require live provider discovery before model config changes."},
    {"risk": "Product smoke is mistaken for full Codex E2E.", "likelihood": "med", "impact": "high", "mitigation": "Document SDK smoke versus interactive Codex distinction in handoff and test plan."},
    {"risk": "Dirty worktree causes unrelated files to be committed.", "likelihood": "med", "impact": "med", "mitigation": "Stage narrowly and review cached diff before commit."}
  ],
  "todos": [
    {"item": "Assign TL/PM/DevOps/QA/Security owners.", "owner": "owner?", "due": "date?"},
    {"item": "Document QA/staging/prod environments or explicitly waive them.", "owner": "owner?", "due": "date?"},
    {"item": "Add CI workflow or document why local-only validation is accepted.", "owner": "owner?", "due": "date?"},
    {"item": "Run fresh-clone install verification.", "owner": "owner?", "due": "date?"},
    {"item": "Resolve real Codex MCP approval path.", "owner": "owner?", "due": "date?"}
  ]
}
```
