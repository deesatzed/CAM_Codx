# Codex-CAM Methodology — Implementation Handoff 2026-05-20

> Resume point after the second implementation session. Phases 0, 1, 2 complete; Phase 3 partial (3.1 + 3.2 done; 3.3, 3.4, 3.5 remaining).

**Worktree:** `/Volumes/WS4TB/WS4TBr/CAM_Codx/codex-cam-methodology-impl` (branch `feature/initial-impl`, pushed to `origin/feature/initial-impl` on https://github.com/deesatzed/CAM_Codx)

**Branch HEAD:** `956536f` (10 commits ahead of `main`)

**main HEAD:** `ec014b1` (untouched)

---

## What's done

### Phase 0 — Preconditions: COMPLETE (with one waiver)
| Task | Status | Artifact |
|---|---|---|
| 0.1 Workspace verification | ✅ | clean tree on `feature/initial-impl` |
| 0.2 Decision log | ✅ | `docs/_decision_log.md` (gitignored; 6 sections now) |
| 0.3 Baselines manifest | ✅ | `baselines/manifest.json` (committed `d6b24d4`) |
| 0.4 Legacy MCP RSS | ✅ | 62,554,112 bytes peak; plan amended for `server` import bug |
| 0.5 Schema snapshot | ✅ | 873-line SQL dump; 8 dependency tables verified present |
| 0.6 Failure corpus | ✅ waiver | 0 real failures available; waiver recorded |

### Phase 1 — Package scaffolding: COMPLETE
| Task | Commit |
|---|---|
| 1.1 `pyproject.toml` | `656367e` |
| 1.2 Package skeleton (11 module files) | `fb8d07d` |
| 1.3 Failing 4-tool ceiling test | `b378828` (test is red; turns green in Phase 6 Task 6.3) |

### Phase 2 — Schemas: COMPLETE
| Task | Commit |
|---|---|
| 2.1 Failing schema tests | (uncommitted by design; bundled with 2.2) |
| 2.2 Pydantic v2 schemas | `5729cb6` (12 tests green, 100% coverage on `schemas.py`) |

### Phase 3 — DB layer: PARTIAL (2 of 5 done)
| Task | Status | Commit |
|---|---|---|
| 3.1 Failing fixture test | ✅ | `0027ca9` |
| 3.2 Slice DB builder + fix | ✅ | `4368a98` + `956536f` (corpus reality fix) |
| 3.3 Failing mode-detection test | ⏸️ pending | — |
| 3.4 Mode-detection implementation | ⏸️ pending | — |
| 3.5 Read conn + write lock + outcome schema | ⏸️ pending | — |

### Test state at session end
```
tests/codex_mcp/test_schemas.py       12 passed
tests/codex_mcp/test_fixture_db.py     2 passed
tests/codex_mcp/test_surface_ceiling.py 1 failed (red — empty REGISTERED_TOOLS, turns green in Phase 6 Task 6.3)
```
**14 passed, 1 failed (expected red).** No flakes. No skips.

---

## Plan bugs found and fixed (this session)

Five amendments now in `docs/_decision_log.md`:

| # | Task | Bug | Fix |
|---|---|---|---|
| 1 | Task 0.4 | `from claw.mcp_server import server` — symbol not at module scope | Use `inspect.getsource(ClawMCPServer.__init__).count('self.handle_')` |
| 2 | Task 0.5 | Replay-identity unachievable (vec0 / FTS5 shadow tables / sqlite_sequence) | Per-table existence check on 8 dependency tables |
| 3 | Task 0.6 | Zero real failures available on this machine | Three-path mining + waiver protocol; downgrade impact on Claim 3 named |
| 4 | Task 3.2 | `DELETE FROM methodology_embeddings WHERE rowid NOT IN (...)` fails (vec0 doesn't expose rowid); slice size assertion wrong | DELETE by primary key column; `sqlite_vec.load(conn)` before any DELETE; size relaxed to ≤4 MB |
| 5 | Task 3.2 + test_fixture_db | Slice of "top N viable" → all rows have `files_affected = '[]'`; provenance assertion fails | Mix 12 viable + 3 embryonic; test rewritten to "≥1 row with populated provenance" |

Bonus minor fix: Task 1.2 `__main__.py` docstring referenced wrong phase numbers; corrected.

---

## What's NOT done

### Phase 3 remaining (3 tasks)
| Task | What |
|---|---|
| 3.3 | Failing mode-detection test (`tests/codex_mcp/test_db_mode.py`) |
| 3.4 | Implement `detect_mode` + `ModeInfo` in `claw_codex_mcp/db.py` |
| 3.5 | Read connection helper + asyncio write lock + idempotent outcome schema bootstrap |

### Phases 4-10 (≥20 tasks)
Per the plan; none started. Includes:
- Phase 4: `cam_recall` + `cam_provenance` handlers
- Phase 5: decisions index + `cam_decisions_search`
- Phase 6: `cam_record_outcome` (centerpiece — closes the loop; required 100% coverage on write paths)
- Phase 7: MCP wire-up (`__main__.py` stdio server)
- Phase 8: `.codex/config.toml` + `.codex/AGENTS.md` updates (outside this repo)
- Phase 9: Skills (`cam_recall_and_cite`, `rescue_ladder`, `outcome_log`, modify `repo_recon`, rewrite `deepscientist-data-research`)
- Phase 10: E2E gates (provenance, lightness, standalone boot — Claims 2, 5, 6)

---

## How to resume

### Pre-flight checks before resuming

```bash
cd /Volumes/WS4TB/WS4TBr/CAM_Codx/codex-cam-methodology-impl
pwd  # → ...-impl

git status --short                 # → empty (clean)
git rev-parse --abbrev-ref HEAD    # → feature/initial-impl
git log --oneline -3
# → 956536f fix(fixtures): mix embryonic + viable in slice ...
# → 4368a98 test(fixtures): real 15-row slice of claw.db ...
# → 0027ca9 test(fixtures): conftest + slice-DB existence check ...

# Sanity-check the tests are in the expected state:
pytest tests/codex_mcp/ -q
# Expected: 14 passed, 1 failed (surface_ceiling is the expected red).

# Sanity-check the corpus is still in the expected state:
sqlite3 /Volumes/WS4TB/WS4TBr/CAM_Codx/CAM_CAM/data/claw.db \
  "SELECT lifecycle_state, COUNT(*) FROM methodologies GROUP BY lifecycle_state;"
# Expected: embryonic|12, viable|95
```

### Recommended: open a fresh session with `executing-plans`

Hand the new session this prompt:

> Resume execution at Task 3.3 of `docs/plans/2026-05-19-codex-cam-methodology-implementation-plan.md`. Phases 0, 1, 2 complete; Phase 3 partial (3.1, 3.2 done). Read `meta/HANDOFF_LATEST.md` first for the full session state and the 5 plan amendments. Read `docs/_decision_log.md` for the corpus-reality facts. Working in worktree at `/Volumes/WS4TB/WS4TBr/CAM_Codx/codex-cam-methodology-impl` on branch `feature/initial-impl`. Current test state: 14 passed, 1 failed (surface_ceiling is the expected red until Phase 6 Task 6.3). Honor all workspace policies: NO MOCK, NO PLACEHOLDER, NO SIMULATION, NO TIMEFRAMES, NO COST/REVENUE, NO "production ready", validation gates between every step.

### What the next implementer will need to know

1. **The slice DB is at `tests/fixtures/claw_slice.db`** (tracked binary, ~3.6 MB, 12 viable + 3 embryonic). Phase 3 mode-detection tests and Phase 4 handler tests will use it via the `slice_db_path` / `slice_conn` fixtures in `tests/conftest.py`.

2. **`sqlite_vec.load(conn)` is required** before any SQL that touches `methodology_embeddings`. The Python env has `sqlite-vec` 0.1.6; `enable_load_extension(True)` is supported on this Python build (verified pre-execution).

3. **The corpus has two distinct lifecycle subsets** (verified live):
   - viable (95 in live DB, 12 in slice): empty `files_affected`, real `tags`, real `success_count`
   - embryonic (12 in live DB, 3 in slice): populated `files_affected`, real `tags`, zero `success_count`
   Tests that assert on provenance fields must account for this.

4. **`importlib.metadata.version("mcp")`** for SDK version reporting — `mcp.__version__` doesn't exist as an attribute.

5. **Codex CLI version** captured in `baselines/manifest.json` is `codex-cli 0.130.0`. If the SDK behavior diverges later, re-pin.

---

## Open questions for the next session

1. **PRD §8 Claim 3 amendment:** the failure-corpus waiver in `docs/_decision_log.md` requires a PRD update referencing the downgrade. Separate doc-only commit.

2. **Codex auto-fire trigger names** in skill frontmatter (`verb_in_user_message`, `implement_md_step_status_transition`, etc.) — need to verify these are real triggers the pinned Codex CLI version recognizes. Test manually in Phase 9.

3. **The 17-tool MCP situation in CAM_CAM:** unchanged. The earlier-session deletion was reverted; `CAM_CAM` is at `771fc14`. No further action unless this decision is reopened.

4. **Phase 6.4 ("Signal reaches CAM_CAM corpus"):** in standalone mode, outcomes write to `~/.cam_codex_mcp/codex_outcome_log.db`, NOT to claw.db. The validation gate that requires `methodology_bandit_outcomes` count change is connected-mode only. Test 6.4 needs an explicit mode-aware skip path or the validation gate needs splitting.

---

## File inventory snapshot

```
/Volumes/WS4TB/WS4TBr/CAM_Codx/codex-cam-methodology-impl/
├── pyproject.toml                          tracked (Task 1.1)
├── .gitignore                              tracked (with negative carve-outs for manifest, repo_set, slice DB)
├── LICENSE, README.md                      tracked
├── baselines/
│   ├── manifest.json                       TRACKED (committed in d6b24d4)
│   ├── manifest.json.template              tracked
│   ├── repo_set.txt                        tracked (TODO placeholders)
│   ├── legacy_mcp_rss.txt                  gitignored
│   ├── claw_db_schema.snapshot.sql         gitignored
│   └── failures/                           DOES NOT EXIST (waiver)
├── docs/
│   ├── _decision_log.md                    gitignored (read this first!)
│   ├── _validation_gates.md                tracked
│   └── plans/2026-05-19-codex-cam-methodology-implementation-plan.md  amended at e9bdeb3
├── meta/
│   ├── HANDOFF_2026-05-17.md               prior (design-phase)
│   ├── HANDOFF_2026-05-19.md               prior (Phase 0 complete)
│   ├── HANDOFF_2026-05-20.md               THIS FILE
│   └── HANDOFF_LATEST.md                   symlink → 2026-05-20
├── migrations/                             empty (Phase 3.5 will populate)
├── prompts/orient_baseline.txt             tracked
├── src/claw_codex_mcp/
│   ├── __init__.py                         "0.1.0"
│   ├── __main__.py                         NotImplementedError stub (Phase 7 Task 7.2)
│   ├── server.py                           REGISTERED_TOOLS = () (Phase 6 Task 6.3 wires handlers)
│   ├── schemas.py                          ✅ 10 pydantic v2 models, 100% covered
│   ├── db.py                               docstring stub (Phase 3.4/3.5 implements)
│   ├── decisions_index.py                  docstring stub (Phase 5 implements)
│   └── tools/                              4 docstring stubs (recall, provenance, decisions_search, record_outcome)
├── tests/
│   ├── __init__.py, codex_mcp/__init__.py  pytest scaffolding
│   ├── conftest.py                         slice_db_path + slice_conn fixtures
│   ├── fixtures/claw_slice.db              TRACKED binary, 3.6 MB, 12 viable + 3 embryonic
│   └── codex_mcp/
│       ├── test_surface_ceiling.py         1 RED (expected; turns green in Phase 6 Task 6.3)
│       ├── test_schemas.py                 12 GREEN
│       └── test_fixture_db.py              2 GREEN
└── tools/
    ├── baseline_cold_start.sh              prerequisite (preexisting from Phase 0 prep)
    ├── build_slice_db.py                   Task 3.2 builder
    ├── codex_trace_patterns.py             prerequisite
    ├── trigger_schema.json                 prerequisite
    ├── validate_skill_frontmatter.py       prerequisite
    └── README.md                           prerequisite index
```

---

## Workspace policy compliance — verified clean

- **NO MOCK DATA** anywhere. The slice fixture is real rows from the live corpus. The failure-corpus gap is documented as a waiver, not synthesized. Every schema test exercises real pydantic validation.
- **NO TIMEFRAMES** in commit messages or docs.
- **NO COST/REVENUE** estimates anywhere.
- **NO "PRODUCTION READY"** claims; explicit "Phase X complete" framing.
- **Validation gates** honored: each task verified independently before advancing; 5 plan bugs caught + amended.
- **Real APIs only**: pydantic 2.13.3, mcp 1.27.0, sqlite-vec 0.1.6 all real and loadable.

Working tree on `feature/initial-impl` is clean. Branch pushed to `origin/feature/initial-impl` on GitHub.

Branch is **10 commits ahead of main**. None of those commits touch `main`. Merging to main is deferred until at least Phase 6 is complete (the centerpiece — closes the outcome ledger loop).
