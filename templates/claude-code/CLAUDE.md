# CLAUDE.md

Use CAM artifacts as evidence. Do not treat generated packet demos as final
standalone products unless the goal explicitly says so.

## Rules

- Read `CAM_CODEX_GOAL.md` first.
- Keep source repos read-only.
- Work only in the assigned target repo or files.
- Preserve provenance and source receipts.
- Run the requested verification commands.
- Return a structured Markdown report with changed files and command results.
