# Goal: Complete CAM_Codx Control-Plane Tasks 10-13

> Status: COMPLETE (2026-08-18). Current evidence is recorded in
> `docs/reports/2026-08-18-task-10-13-release-audit.md`. Task 14 is successor
> scope and is governed separately by `GOAL_TASK_14_MATRAIX_SESA.md`.

This is the autonomous continuation contract for Tasks 10-13 of
`docs/plans/2026-08-12-cam-codx-control-plane.md`. Tasks 1-9 are prior work;
do not reopen them except to repair a regression directly caused by this goal.

Current recovery workspace:

- CAM_Codx: `/Users/o2satz/Downloads/crash814/worktrees/CAM_Codx`
- CAM_CAM: `/Users/o2satz/Downloads/crash814/worktrees/CAM_CAM`
- Feature branch in both repositories: `feat/cam-codx-control-plane`
- CAM_Codx baseline: Task 8 commit `b0e0c0d`; Task 9 commit `5a3c9fa`.
- CAM_CAM baseline: Task 7 commit `dc37550`.

/goal

OUTCOME: Complete Tasks 10-13 so CAM_Codx routes mining and supported CAM
administration through the pinned registry and fixed manager packets, normal
documentation presents CAM_Codx as the single user-facing control plane, the
two repositories pass their current release gates, and a deterministic fixture
chain proves that only a verified result is eligible for positive later reuse.

PROOF OF DONE:

1. Task 10 has RED-then-GREEN tests covering explicit route scope for mining,
   knowledge, models, benchmark, self-enhancement, evolution, doctor, and
   setup. Each manager packet is fixed list-form, selected from the one
   registry, and cannot silently authorize another route or side effect.
2. Mining composition reuses the existing `cam_pull_mine_dir.py` coordinator,
   secure wrapper, and CAM_CAM runtime. It accepts an explicit source, corpus,
   time, and cost bound; captures a receipt path and SHA-256; reports
   corpus/ledger delta; and stops before build selection. CAM_Codx contains no
   provider client or duplicate CAM runtime/mining implementation.
3. Model/profile promotion, rollback, self-enhancement swap, live CAM changes,
   configuration writes, provider spend, and mining each remain distinct,
   scoped approval classes. No live or paid operation is run as fixture proof.
4. Task 11 documentation-contract tests prove normal CAM_Codx help,
   quickstart, cheatsheet, status, generated packs, and manager documentation
   begin with outcome language such as `Use CAM_Codx to ...`. CAM_CAM directs
   expert CLI users to troubleshooting, runtime development, recovery,
   regression isolation, or expert integration. No documentation claims that
   `cam chat` is a complete general router.
5. Generated packs are current and normal documentation has no competing
   legacy skill UX or stale active paths. Do not remove approved-but-unbuilt
   notices until their underlying implementation and cross-repo verification
   genuinely pass.
6. Task 12 focused and full suites run in both recovery checkouts. The stale
   plan path `tests/planning/test_application_packet.py` is replaced by the
   actual `tests/test_application_packet.py`. Caused regressions are fixed;
   unrelated baseline failures are recorded with exact output and are not
   described as success.
7. A temporary Codex home installation proves only the canonical `cam-codx`
   skill is newly installed. A read-only `assess` fixture proves target,
   corpus, model-profile, and configuration identities do not change.
8. Task 13 adds and passes `tests/test_cam_control_plane_e2e.py` plus
   `docs/reports/2026-08-12-cam-control-plane-fixture-proof.md`. The fixture
   sequence covers assessment, candidate decisions, reviewed plan, simulated
   landing, failed verification, corrected verification, outcome recording,
   and later recall. Only the verified active result may be positive reuse
   evidence; rejected, deferred, failed, synthetic, and hypothetical records
   remain distinguishable.
9. `PROGRESS.md` records each batch's changed files, commands, receipts,
   assumptions, limitations, and next action. `DECISIONS.md` records material
   policy choices. `git diff --check` passes in both repositories, and each
   owning-repository commit is pushed to the feature branch.

SCOPE:

- CAM_Codx implementation: `tools/cam_control_plane.py`,
  `tools/cam_pull_mine_dir.py`, `tools/cam_manager.py`, the canonical skill,
  setup/install helpers, registry generator, tests, reports, and user docs
  explicitly needed by Tasks 10-13.
- CAM_CAM changes are limited to tests, CLI/runtime truth, or documentation
  directly required by a cross-repository regression or Task 11 contract.
- Read and compose existing CAM_CAM runtime behavior; do not copy it into
  CAM_Codx.
- Use only the recovered isolated checkouts above. The read-only SSD worktrees
  and their `claw.db`, `claw.toml`, model profiles, and deployed services are
  out of bounds.
- Task 14 and any MatrAIx/SESA target-repository edit are out of scope.

CONSTRAINTS:

- Read `GOAL.md`, `STANDARDS.md`, `IMPLEMENT.md`, `DECISIONS.md`,
  `PROGRESS.md`, and `TASK_QUEUE.md` when present before each repository phase.
- Start every behavior change with a failing regression test; do not weaken,
  delete, skip, or relabel tests to obtain green output.
- Use the existing machine-readable capability registry as the only route
  catalogue. Unknown, drifted, hidden-compatibility, or ambiguous routes fail
  closed.
- Use exact list-form subprocess argv and the approved wrapper. No shell
  execution paths, secrets in arguments, provider clients, runtime forks,
  schema migrations, dependency additions, broad cleanup, or public API breaks
  unless a specific task proves necessity and records the decision.
- Fixture and synthetic evidence is never live-product proof, real-world
  accuracy, provider validation, or production readiness.
- Preserve all negative evidence: failed verification, partial runs, rejected
  candidates, deferred candidates, limitations, and abandoned work cannot
  create positive trust or recipe eligibility.

AUTONOMOUS APPROVAL POLICY:

- Continue without user approval for routine inspection, tests, implementation,
  formatting, generated-document refresh, review remediation, truth-document
  updates, commits, and feature-branch pushes.
- Read-only assessment needs no product approval after runtime identities are
  resolved.
- A named bounded local-record or target-mutation authorization covers only its
  exact packet; it never authorizes mining, provider spend, promotion, profile
  change, configuration change, live CAM mutation, deployment, or destruction.
- Live mining or provider spend requires explicit source, corpus/database,
  model, time, and cost authorization. Promotion, rollback, swap, deployment,
  destructive action, and live configuration/database mutation remain separate
  explicit approvals.

SAFETY / PROVENANCE:

- Before any non-read-only route, resolve and display the exact CAM_Codx and
  CAM_CAM checkouts, wrapper, config, primary database, model-profile file,
  target repository, and immutable revision.
- Fail closed on identity ambiguity, registry/manifest drift, missing or stale
  approval, secret-bearing arguments, missing receipt, digest mismatch,
  mutable revision label, unknown route, or incomplete verification gate.
- Hash and parse every receipt from the same captured byte buffer. Bind
  verification to the exact managed-plan and target identities.
- Preserve source receipts and license information. Keep direct precedent,
  transferable analogy, and new hypothesis separate in all fixture reports.
- Do not expose secrets, private keys, credentials, sensitive source, or
  unnecessary personal data to logs, documents, packets, providers, or agents.

ITERATION:

1. Reconcile current recovery branch state and truth files. Record the exact
   command/runtime identity before modifying either repository.
2. Execute Task 10 in small TDD batches: add one RED route/authorization test,
   implement minimal composition, run focused router/pull-mine/manager tests,
   then document and commit. Do not invoke a live mining or administration
   route merely to prove packet construction.
3. Execute Task 11 with failing documentation-contract tests first. Update
   documentation and generated artifacts from the registry; run focused docs,
   skill, setup, manifest/UX, and generator checks; commit each repository
   separately.
4. Execute Task 12 release gates against actual recovery paths. Correct the
   stale application-packet test reference. Run focused gates, full suites,
   registry/manifest validation, generated-output checks, and temporary-skill
   installation/read-only identity proof. Record every baseline issue exactly.
5. Execute Task 13 in deterministic local fixtures only. First demonstrate
   failed verification cannot produce a positive recalled result; then correct
   verification and prove later recall distinguishes the verified active result
   from all negative/deferred/hypothetical records. Write the fixture report.
6. After each batch, inspect diffs, run nearest tests, update truth documents,
   run `git diff --check`, commit scoped work, and push the owning feature
   branch. Diagnose failures with a different approach after each unsuccessful
   repair attempt.

STOP:

Pause and report the exact blocker only if credentials, account access, license
rights, private data access, or an explicit authorization required for a live
operation is missing; Task 12 required verification has no safe local/fixture
alternative; a material safety, privacy, licensing, or product-scope issue is
discovered; or the same blocking condition persists after three documented,
distinct repair strategies. Do not stop for ordinary RED tests, review
findings, sandbox cache warnings, documentation work, or routine repair.

COMPLETE:

Mark this goal complete only when every proof item is supported by current
command output and inspected artifacts, Tasks 10-13 have scoped commits pushed
in their owning repositories, both worktrees pass `git diff --check`, release
gate failures (if any) are accurately classified, temporary skill installation
and read-only identity proof pass, and the Task 13 report explicitly calls its
result fixture proof rather than live-product proof. The next action after
completion is to draft the separate Task 14 product slice contract and pause
at its required product-boundary decision.
