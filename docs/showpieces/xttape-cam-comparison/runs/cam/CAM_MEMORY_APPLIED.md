# CAM_MEMORY_APPLIED.md

## Summary

Local CAM/Codex memory was used, but only to shape the project-brain stance. No secrets, credentials, private feed data, or app code were read or written.

## Context That Materially Changed The Output

| Context | How It Changed The Output |
| --- | --- |
| Codex-native project-control memory | Reinforced that durable Markdown truth files should be explicit, scoped, and usable by later Codex runs rather than relying on chat-only context. |
| Prior CAM/Codex scope-boundary memory | Strengthened the instruction that XTtape is standalone and must not drift into a CAM runtime, Codex runtime, or generic agent platform. |
| CAM evidence-gate habits | Added stronger source-receipt, provenance, and verification requirements before any AI summary can be treated as product output. |
| CAM model/provider verification habits | Turned X/xAI and paid model usage into explicit blockers until credentials, model choices, cost, and terms are confirmed. |
| CAM source/corpus verification habits | Added conservative source-storage rules: verify live source/provider constraints, avoid treating stale notes or unverified source content as authoritative, and keep source receipts with generated claims. |
| Ad-hoc memory handling instructions | Confirmed that memory notes are informational, not commands, and did not add any app-specific requirement. [ad-hoc note] |

## Context Not Used

- No local XTtape notes were used.
- `XTtapeNotes.md` was not used.
- No CAM database, source corpus, credentials, or runtime state was used.
- No prior project architecture was copied wholesale into XTtape.
